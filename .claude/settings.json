{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "shell": "powershell",
            "statusMessage": "Sincronizando a biblioteca de design...",
            "timeout": 60,
            "command": "$p = if ($env:CLAUDE_PROJECT_DIR) { $env:CLAUDE_PROJECT_DIR } else { (Get-Location).Path }; $s = Join-Path $p 'ferramentas\\sincronizar-biblioteca.ps1'; if (Test-Path $s) { & powershell -NoProfile -ExecutionPolicy Bypass -File $s } else { Write-Output 'Aviso: abra a sessao dentro da pasta do banco-demandas para a biblioteca sincronizar sozinha.' }"
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "shell": "powershell",
            "statusMessage": "Guardiao do offline conferindo...",
            "timeout": 45,
            "command": "$p = if ($env:CLAUDE_PROJECT_DIR) { $env:CLAUDE_PROJECT_DIR } else { (Get-Location).Path }; $s = Join-Path $p 'ferramentas\\guardiao-offline.ps1'; if (Test-Path $s) { & powershell -NoProfile -ExecutionPolicy Bypass -File $s; exit $LASTEXITCODE }"
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Bash|PowerShell",
        "hooks": [
          {
            "type": "command",
            "shell": "powershell",
            "if": "Bash(git commit*)",
            "statusMessage": "Guardiao da versao conferindo antes de publicar...",
            "timeout": 45,
            "command": "$p = if ($env:CLAUDE_PROJECT_DIR) { $env:CLAUDE_PROJECT_DIR } else { (Get-Location).Path }; $s = Join-Path $p 'ferramentas\\guardiao-versao.ps1'; if (Test-Path $s) { & powershell -NoProfile -ExecutionPolicy Bypass -File $s; exit $LASTEXITCODE }"
          }
        ]
      }
    ]
  }
}
