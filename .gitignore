# Arquivos com dados reais — vivem no repo privado banco-demandas-dados
CONTINUIDADE.md
PROMPT-PROXIMO-CHAT.md

# Backups e dados exportados
- BACKUPS/
banco.json
*.bak

# Cargas de dados reais das lojas (itens de manutencao, NC, checklist).
# Entram no site UMA vez e passam a viver no banco dela + repo privado.
# NUNCA versionar: este repositorio e publico.
dados/

# Ambiente
.DS_Store
Thumbs.db

# Pasta tecnica: guardamos so o que precisa viajar com o projeto
# (o que faz as regras de design rodarem sozinhas em qualquer computador).
# ATENCAO: tem de ser ".claude/*" e nao ".claude/" — com a barra o git nem olha
# dentro da pasta e as excecoes abaixo nao funcionam.
.claude/*
!.claude/settings.json
!.claude/launch.json
!.claude/agents/
!.claude/skills/

# dados de trabalho do modo rascunho: sao dados reais dela, nunca entram no repositorio publico
_dados-rascunho.json
_teste-*.json

# arquivos que o gerador da folha cria e apaga sozinho; se um sobrar por causa
# de um erro no meio do caminho, ele NAO pode ir para o repositorio publico:
# tem dado real dela dentro
dados-da-folha.js
_folha-em-montagem.html
_folha-montada.html

# comparacoes visuais temporarias (mockups que a Le olha na sessao)
_comparacoes/
